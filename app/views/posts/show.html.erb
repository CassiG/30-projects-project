<%= link_to "About the Project", about_path%>
</div>
<div class="clear"></div>

<div class="mini-nav">
  <% if session[:user_id] %>
    <%= link_to "Edit", edit_post_path(@post), class: "post-edit" %> /
    <%= link_to "Delete", post_path(@post), method: :delete, data: {confirm: "Really delete the article?"}, class: "post-edit" %>
  <% end %>
</div>
<h2> <%= @post.title %> </h2>
<p> <%= @post.body %> </p>
<p> <% @post.tags.each do |tag| %>
    <%= link_to tag.name, tag_path(tag), class: 'tag' %>
  <% end %></p>
